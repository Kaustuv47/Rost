[package]
name    = "rost-init"
version = "0.1.0"
edition = "2021"

# No kernel crate dependencies.
# The only interface to the kernel is the syscalls in src/syscall.rs.

[[bin]]
name = "rost-init"
path = "src/main.rs"
