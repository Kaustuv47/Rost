[build]
# Freestanding x86_64 — no OS ABI, no libc, no std.
target = "x86_64-unknown-none"

[target.x86_64-unknown-none]
rustflags = [
    "-C", "link-arg=-nostdlib",
    "-C", "no-redzone",
    "-C", "relocation-model=static",
]
